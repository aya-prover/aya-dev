public open data Nat
| zero
| suc Nat

def overlap infixl + (a b : Nat) : Nat
| zero, a => a
| a, zero => a
| suc a, b => suc (a + b)
| a, suc b => suc (a + b)


example def tacHeadList: Nat => tactic {
  { | {??}
    | suc zero
  }
  { | suc {??} | zero }
  | zero
}
