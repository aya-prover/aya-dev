def negTwo : Int => neg two
def negOne : Int => neg one

def two : Nat => suc one
def one : Nat => suc zero

open data Int : Type
  | pos Nat
  | neg Nat {
    | zero => pos zero
  }
open data Nat : Type | zero | suc Nat
