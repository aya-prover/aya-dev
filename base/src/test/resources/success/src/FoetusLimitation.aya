open data Nat | zero | suc Nat
open data List (A : Type) : Type | nil | infixr cons A (List A)

def sum (List Nat) : Nat
  | nil => zero
  | suc x cons xs => suc (sum (x cons xs))
  | zero cons xs => sum xs
