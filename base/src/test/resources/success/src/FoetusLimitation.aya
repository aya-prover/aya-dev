open data Nat | zero | suc Nat
open data List (A : Type) : Type | nil | cons A (List A)

def sum (List Nat) : Nat
  | nil => zero
  | cons (suc x) xs => suc (sum (cons x xs))
  | cons zero xs => sum xs
