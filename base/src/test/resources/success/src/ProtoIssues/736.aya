open class PointedMagma
  | A : Type
  | point : A
  | op : A -> A -> A

open data Unit | unit
def test : Fn {x : Type} -> Type => \{x} => PointedMagma {x}

def unitMagma : PointedMagma {Unit} => new test { | point => unit | op => \ x y => unit }
