open import Nat::Core
open import Path
open import StringPrims using (
    String
  , strcat as infixl ++
)

def hello1 : Nat => 114
def hello2 : Nat => 514
def hello3 : String => "1919" ++ "810"

module Subworld {
  def foo (a : Nat) {b : Nat} Nat : Nat
  | zero, _ => let c (d : Nat) (e : Nat) : Nat := d in c a a
  | suc a, _ => let b := "114" in a
}

variable A B : String

def justTest => do { }
